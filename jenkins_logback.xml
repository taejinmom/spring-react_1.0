<configuration>

    <!-- Console appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="ERROR">
        <appender-ref ref="CONSOLE" />
    </root>

    <!-- Set logging level for specific packages -->
    <logger name="org.jenkinsci" level="WARN" />
    <logger name="hudson" level="WARN" />
    <logger name="ssh" level="ERROR" /> <!-- Change to ERROR or OFF to reduce logging -->
    
</configuration>
